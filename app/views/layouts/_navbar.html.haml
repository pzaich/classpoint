.container
	.navbar.navbar-fixed-top
		.navbar-inner
			=link_to 'Classpoint', root_path, :class => 'brand'
			%ul.nav.pull-right.login
				%li
					-if !user_signed_in?
						=link_to "Sign in", new_user_session_path
					-else
						=link_to "Sign Out", destroy_user_session_path, :method => :delete

